<Window x:Class="Course.View.DocumentViewerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Course.View"
        mc:Ignorable="d"
        Title="DocumentViewerWindow" Height="450" Width="600">
    <Grid>
        <!--<Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <DocumentViewer x:Name="documentViewer" Document="{Binding Manual}" >

 
        </DocumentViewer>
        <StackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Center">
            <Button Content="Сохранить" />
            <Button Content="Загрузить" />
        </StackPanel>-->
        <FlowDocumentScrollViewer MinZoom="50" MaxZoom="1000" 
                            ZoomIncrement="5" Zoom="100" IsToolBarVisible="True" >
            </FlowDocumentScrollViewer>
    </Grid>
</Window>
